@model IEnumerable<TranThiLua0681.Models.TTL50681>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TTLName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TTLGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TTLName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TTLGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TTLId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TTLId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TTLId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
